services:
  frontend:
    container_name: frontend
    image: ghcr.io/${REPO_OWNER?REPO_OWNER not set}/frontend:${IMAGE_TAG:-latest}
    ports:
      - "3000:3000"
    environment:
      GRAPHQL_URL: ${GRAPHQL_URL:-http://172.17.0.1:4000/graphql/query}
